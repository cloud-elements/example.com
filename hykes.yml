---

### CLOUD PHASE ###############################################################
cloud:
  # Environment variables made available on all servers.
  env:
    - BASE_FQDN='example.com'
    - ELASTICSEARCH_VERSION='1.7.3'
    - KAFKA_VERSION='0.8.2.1'
    - LOGSTASH_VERSION='1.5.4'
    - MAVEN_VERSION='3.3.3'
    - MEMCACHED_VERSION='1.4.24'
    - NGINX_VERSION='1.8.0'
    - POSTGRESQL_VERSION='9.3.9'
    - REDIS_VERSION='3.0.4'
    - REPMGR_VERSION='3.0.2'
    - TOMCAT_VERSION='7.0.64'

  # Secret variables only made available during build tasks.
  secret:
    - POSTGRESQL_BACKUP_ENCRYPTION_PASSWORD='backup'
    - POSTGRESQL_CESECURITY_PASSWORD='cesecurity'
    - POSTGRESQL_ELEMENTS_PASSWORD='elements'
    - POSTGRESQL_MASTER_ENCRYPTION_PASSWORD='master'

  # Lifecycle hook which runs before all tasks.
  pre:

  # Lifecycle hook which runs after all tasks.
  post:

### APP PHASE #################################################################
app:
  # Environment variables made available on servers with role.
  env:

  # Secret variables only made available during build tasks.
  secret:

  # Lifecycle hook which runs before app build tasks.
  pre:

  # Lifecycle hook which runs after app build tasks.
  post:

### CACHE PHASE ###############################################################
cache:
  # Environment variables made available on servers with role.
  env:

  # Secret variables only made available during build tasks.
  secret:

  # Lifecycle hook which runs before cache build tasks.
  pre:

  # Lifecycle hook which runs after cache build tasks.
  post:

### DATABASE PHASE ############################################################
database:
  # Environment variables made available on servers with role.
  env:

  # Secret variables only made available during build tasks.
  secret:

  # Lifecycle hook which runs before database build tasks.
  pre:

  # Lifecycle hook which runs after database build tasks.
  post:

### EIB PHASE #################################################################
eib:
  # Environment variables made available on servers with role.
  env:

  # Secret variables only made available during build tasks.
  secret:

  # Lifecycle hook which runs before eib build tasks.
  pre:

  # Lifecycle hook which runs after eib build tasks.
  post:

### LOADBALANCE PHASE #########################################################
loadbalance:
  # Environment variables made available on servers with role.
  env:

  # Secret variables only made available during build tasks.
  secret:

  # Lifecycle hook which runs before loadbalance build tasks.
  pre:

  # Lifecycle hook which runs after loadbalance build tasks.
  post:

### LOGPIPE PHASE #############################################################
logpipe:
  # Environment variables made available on servers with role.
  env:

  # Secret variables only made available during build tasks.
  secret:

  # Lifecycle hook which runs before logpipe build tasks.
  pre:

  # Lifecycle hook which runs before logpipe build tasks.
  post:

### LOGSEARCH PHASE ###########################################################
logsearch:
  # Environment variables made available on servers with role.
  env:

  # Secret variables only made available during build tasks.
  secret:

  # Lifecycle hook which runs before logsearch build tasks.
  pre:

  # Lifecycle hook which runs after logsearch build tasks.
  post:
